[project]
name = "blender-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=3.4.4",
    "mcp[cli]>=1.28.1",
]

[project.scripts]
blender-mcp = "blender_mcp:mcp.run"

