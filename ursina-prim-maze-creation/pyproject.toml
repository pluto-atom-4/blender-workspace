[project]
name = "ursina-prim-maze-creation"
version = "0.1.0"
description = "Real-time 3D animated maze generation (Prim's Algorithm) on the Ursina Engine"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "panda3d>=1.10.16",
    "ursina>=8.3.0",
]

[project.optional-dependencies]
test = [
    "pytest>=9.1.1",
]
